describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { FrontendCoreModule } from './core.module';

describe('FrontendCoreModule', () => {
  it('should work', () => {
    expect(new FrontendCoreModule(null)).toBeDefined();
  });
});
*/
